#include<iostream>
#include<string>
#include<vector>
#include<algorithm>
using namespace std;

long long T, N, ans, x1, x2, y1, y2;
int main() {
	scanf("%lld", &T);
	for (int i = 0; i < T; i++) {
		scanf("%lld %lld %lld %lld", &x1, &x2, &y1, &y2);
		long long x = x2 - x1 + 1;
		long long y = y2 - y1 + 1;
		ans = 
	}
	printf("%lld\n", ans);
	return 0;
}
