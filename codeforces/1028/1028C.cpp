#include<iostream>
#include<algorithm>
#include<string>
#include<vector>
using namespace std;

int N, a[200006], b[200006], c[200006], d[200006];
int main() {
    scanf("%d", &N);
    for(int i = 0; i < N; i++) {
    	scanf("%d %d %d %d", &a[i], &b[i], &c[i], &d[i]);
    }
    if ()
    printf("%d\n", );
    return 0;
}
