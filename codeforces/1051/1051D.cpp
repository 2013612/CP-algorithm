#include<iostream>
#include<algorithm>
#include<vector>
#include<string>
using namespace std;

int N, a[], ans;
int main(){
	scanf("%d", &N);
	for (int i = 0; i < N; i++) {
		scanf("%d", &a[i]);
	}
	printf("%d\n", ans);
	return 0;
}
